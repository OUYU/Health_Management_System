@model List<AIPProject01.Models.ViewModel.G3_SystemAdministrator_HealthLink>
@{
    ViewBag.Title = "身心健康與安全管理系統";
}

@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title</h1>
                <h2>@ViewBag.Message</h2>
            </hgroup>
            @*<p>
                    To learn more about ASP.NET MVC visit
                    <a href="http://asp.net/mvc" title="ASP.NET MVC Website">http://asp.net/mvc</a>.
                    The page features <mark>videos, tutorials, and samples</mark> to help you get the most from ASP.NET MVC.
                    If you have any questions about ASP.NET MVC visit
                    <a href="http://forums.asp.net/1146.aspx/1?MVC" title="ASP.NET MVC Forum">our forums</a>.
                </p>*@
            <p>
            </p>
        </div>
    </section>
}

<div class="container">
    <div class="table-responsive" style="overflow:auto; height: 650px; border: 2px #ccc solid; padding: 10px">
        <header style="font-size:20px;">
            @{
                if (@Model != null)
                {
                    <h3>各大醫院衛教文章連結</h3>
                    <table class="table">
                        <tr>
                            <th class="col-sm-1">連結名稱</th>
                            <th class="col-sm-1">新增者</th>
                            <th class="col-sm-1">新增時間</th>
                        </tr>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="active">
                                    <a href="@item.Content">@item.Title</a>
                                </td>
                                <td class="info">
                                    @item.UserId
                                </td>
                                <td class="warning">
                                    @item.Date
                                </td>
                            </tr>
                        }
                    </table>
                }
                else
                {
                    <h3>系統維護中，請稍後，管理員正盡速處理</h3>
                }
            }
        </header>
    </div>
</div>

